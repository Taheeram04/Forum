services:
  forum:
    build: .
    ports:
      - "8080:8080"
    volumes:
      - forum-data:/app/data
    environment:
      - PORT=8080
      - DB_PATH=/app/data/forum.db
    restart: unless-stopped

volumes:
  forum-data:
